# إضافة مدة تجريبية 6 أيام للعروض وإضافة منتجات العروض

## الوصف
هذا الملف يشرح كيفية إضافة مدة تجريبية 6 أيام للعروض وإضافة منتجات العروض.

## الخطوات

### 1. تشغيل الـ Seeder
قم بتشغيل الأمر التالي في terminal:

```bash
cd web
php artisan db:seed --class=AddTrialPeriodOffersSeeder
```

### 2. ما الذي يفعله الـ Seeder؟

- **تحديث جميع العروض الموجودة**: يحدّث جميع العروض لتكون نشطة مع مدة تجريبية 6 أيام (start_date = الآن، end_date = الآن + 6 أيام)
- **إضافة عروض جديدة**: إذا كان عدد العروض النشطة أقل من 8، سيتم إضافة عروض جديدة
- **إضافة منتجات العروض**: يضيف منتجات عروض جديدة (منتجات مع original_price) إذا كان عددها أقل من 20

### 3. التحقق من النتائج

بعد تشغيل الـ Seeder، يمكنك التحقق من:
- صفحة العروض في الواجهة الأمامية يجب أن تعرض العروض
- جميع العروض يجب أن تكون نشطة وتنتهي بعد 6 أيام من الآن
- يجب أن تكون هناك منتجات عروض متاحة

### 4. ملاحظات

- تأكد من وجود:
  - أقسام العروض (Offer Categories)
  - Vendors
  - Categories
  - Subcategories
  - Brands

إذا لم تكن موجودة، قم بتشغيل الـ Seeders المناسبة أولاً.

## استكشاف الأخطاء

إذا واجهت مشاكل:
1. تأكد من أن قاعدة البيانات متصلة
2. تأكد من وجود جميع الجداول المطلوبة
3. تحقق من السجلات في جدول `offers` و `products`
4. تحقق من أن `is_active = true` و `end_date` في المستقبل

