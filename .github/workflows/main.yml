name: Deploy & Sync Files

on:
  push:
    branches:
      - main
    paths:
      - 'admin/**'
      - 'api/**'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Deploy Admin Files via SFTP
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ secrets.SFTP_PORT }}
          uploads: |
            ./admin/ => /home/u929533639/domains/eliteonegrocery.com/public_html/adminxd/
          delete: false

      - name: Deploy API Files via SFTP
        uses: Dylan700/sftp-upload-action@latest
        with:
          server: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ secrets.SFTP_PORT }}
          uploads: |
            ./api/ => /home/u929533639/domains/eliteonegrocery.com/public_html/ad/
          delete: false

      - name: Run Database Migrations & Seeders
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ secrets.SFTP_PORT }}
          script: |
            echo "🚀 Starting database operations..."
            
            # التنفيذ من مسار adminxd
            cd /home/u929533639/domains/eliteonegrocery.com/public_html/adminxd
            
            echo "📦 Running migrations..."
            php artisan migrate --force
            
            if [ $? -eq 0 ]; then
              echo "✅ Migrations completed successfully"
            else
              echo "❌ Migrations failed"
              exit 1
            fi
            
            echo "🌱 Running seeders..."
            php artisan db:seed --force
            
            if [ $? -eq 0 ]; then
              echo "✅ Seeders completed successfully"
            else
              echo "⚠️ Seeders failed or no seeders found"
            fi
            
            echo "🎉 All operations completed!"

      - name: Clear Cache
        uses: appleboy/ssh-action@master
        with:
          host: ${{ secrets.SFTP_HOST }}
          username: ${{ secrets.SFTP_USERNAME }}
          password: ${{ secrets.SFTP_PASSWORD }}
          port: ${{ secrets.SFTP_PORT }}
          script: |
            echo "🧹 Clearing cache..."
            cd /home/u929533639/domains/eliteonegrocery.com/public_html/adminxd
            php artisan cache:clear
            php artisan config:clear
            php artisan route:clear
            php artisan view:clear
            echo "✅ Cache cleared successfully"

      - name: Deployment Summary
        run: |
          echo "✅ Deployment completed successfully!"
          echo "📁 Admin files → /public_html/adminxd/"
          echo "📁 API files → /public_html/ad/"
          echo "🗄️ Database migrations executed"
          echo "🌱 Database seeders executed"
          echo "🧹 Cache cleared"
